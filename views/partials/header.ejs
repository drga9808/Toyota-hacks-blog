<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Toyota Hacks</title>

    <!-- CSS Style -->
    <link rel="stylesheet" href="/styles/main.css">
    <!-- Local Bootstrap CSS -->
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <!-- Montserrat Font-Family -->
    <link href='https://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet'>
    <!-- Font Awasome (Icons) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- DOMPurify -->
    <script src="https://cdn.jsdelivr.net/npm/dompurify@3.0.5/dist/purify.min.js"></script>

</head>

<body>

    <header class="mb-5 pb-5">

        <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
            <div class="container d-flex justify-content-between align-items-center">

                <!-- Brand on the left (but centered visually on large screens) -->
                <a class="navbar-brand me-lg-auto" href="/">Toyota Hacks Blog</a>

                <!-- Toggler (mobile only) -->
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu"
                    aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Nav items aligned to the right on large screens -->
                <div class="collapse navbar-collapse justify-content-lg-end mb-3 mb-md-0" id="navbarMenu">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/#all-posts">Read All Posts</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/create-post">Create Post</a>
                        </li>
                        <% if (currentUser) { %>
                            <li class="nav-item dropdown ms-lg-4">
                                <a class="nav-link dropdown-toggle text-white" href="#" id="userMenu" role="button"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                    👤  <%= users.find(u=> u.email === currentUser)?.name || currentUser %>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow-sm w-50" aria-labelledby="userMenu">
                                    <li>
                                        <a class="dropdown-item d-flex align-items-center gap-2 text-danger"
                                            href="/logout">
                                            <i class="fas fa-sign-out-alt"></i> Logout
                                        </a>
                                    </li>
                                </ul>
                            </li>
                            <% } else { %>
                                <li class="nav-item ms-lg-4">
                                    <a class="btn btn-outline-light btn-sm px-3 py-1 mt-1" href="/login">
                                        <i class="fas fa-sign-in-alt me-1"></i> Login
                                    </a>
                                </li>
                                <% } %>
                    </ul>
                </div>

            </div>
        </nav>

    </header>